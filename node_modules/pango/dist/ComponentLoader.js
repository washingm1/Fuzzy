"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var path = require("path");
var fs = require("fs-extra");
var NestedError = require("nested-error-stacks");
var ComponentLoader = /** @class */ (function () {
    function ComponentLoader() {
    }
    ComponentLoader.loadComponents = function (projectOptions) {
        return __awaiter(this, void 0, void 0, function () {
            var componentPaths, _i, componentPaths_1, componentPath;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this.getComponentPaths(projectOptions)];
                    case 1:
                        componentPaths = _a.sent();
                        _i = 0, componentPaths_1 = componentPaths;
                        _a.label = 2;
                    case 2:
                        if (!(_i < componentPaths_1.length)) return [3 /*break*/, 5];
                        componentPath = componentPaths_1[_i];
                        return [4 /*yield*/, ComponentLoader.loadComponent(projectOptions, componentPath)];
                    case 3:
                        _a.sent();
                        _a.label = 4;
                    case 4:
                        _i++;
                        return [3 /*break*/, 2];
                    case 5: return [2 /*return*/];
                }
            });
        });
    };
    ComponentLoader.getComponentPaths = function (projectOptions) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!projectOptions.components) return [3 /*break*/, 2];
                        return [4 /*yield*/, ComponentLoader.findComponentPathsFromList(projectOptions, projectOptions.projectDir, projectOptions.components)];
                    case 1: return [2 /*return*/, _a.sent()];
                    case 2: return [4 /*yield*/, ComponentLoader.findComponentPaths(projectOptions.projectDir)];
                    case 3: return [2 /*return*/, _a.sent()];
                }
            });
        });
    };
    ComponentLoader.findComponentPathsFromList = function (projectOptions, projectDir, components) {
        return __awaiter(this, void 0, void 0, function () {
            var results, _i, components_1, component, componentName, searchPaths, found, _a, searchPaths_1, searchPath, fullPath, _b, message, _c, searchPaths_2, searchPath;
            return __generator(this, function (_d) {
                switch (_d.label) {
                    case 0:
                        results = [];
                        _i = 0, components_1 = components;
                        _d.label = 1;
                    case 1:
                        if (!(_i < components_1.length)) return [3 /*break*/, 11];
                        component = components_1[_i];
                        if (!(typeof component === 'string')) return [3 /*break*/, 9];
                        componentName = component;
                        searchPaths = [
                            componentName,
                            path.join(componentName, 'pango-component.js'),
                            path.join('node_modules', componentName),
                            path.join('node_modules', componentName, 'pango-component.js')
                        ];
                        found = false;
                        _a = 0, searchPaths_1 = searchPaths;
                        _d.label = 2;
                    case 2:
                        if (!(_a < searchPaths_1.length)) return [3 /*break*/, 8];
                        searchPath = searchPaths_1[_a];
                        fullPath = path.join(projectDir, searchPath);
                        return [4 /*yield*/, fs.pathExists(fullPath)];
                    case 3:
                        _b = (_d.sent());
                        if (!_b) return [3 /*break*/, 5];
                        return [4 /*yield*/, fs.stat(fullPath)];
                    case 4:
                        _b = (_d.sent()).isFile();
                        _d.label = 5;
                    case 5:
                        if (!_b) return [3 /*break*/, 7];
                        return [4 /*yield*/, ComponentLoader.loadComponent(projectOptions, searchPath)];
                    case 6:
                        _d.sent();
                        found = true;
                        return [3 /*break*/, 8];
                    case 7:
                        _a++;
                        return [3 /*break*/, 2];
                    case 8:
                        if (!found) {
                            message = "Could not find component \"" + component + "\"\n";
                            for (_c = 0, searchPaths_2 = searchPaths; _c < searchPaths_2.length; _c++) {
                                searchPath = searchPaths_2[_c];
                                message += "  - " + searchPath + "\n";
                            }
                            throw new Error(message);
                        }
                        return [3 /*break*/, 10];
                    case 9:
                        results.push(component);
                        _d.label = 10;
                    case 10:
                        _i++;
                        return [3 /*break*/, 1];
                    case 11: return [2 /*return*/, results];
                }
            });
        });
    };
    ComponentLoader.findComponentPaths = function (rootDir, relativePath) {
        return __awaiter(this, void 0, void 0, function () {
            var results, dir, pangoIgnoreFileName, dirEntries, err_1, _i, dirEntries_1, dirEntry, p, stat, r;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        relativePath = relativePath || '.';
                        results = [];
                        dir = path.join(rootDir, relativePath);
                        pangoIgnoreFileName = path.join(dir, '.pangoignore');
                        return [4 /*yield*/, fs.pathExists(pangoIgnoreFileName)];
                    case 1:
                        if (_a.sent()) {
                            return [2 /*return*/];
                        }
                        _a.label = 2;
                    case 2:
                        _a.trys.push([2, 4, , 5]);
                        return [4 /*yield*/, fs.readdir(dir)];
                    case 3:
                        dirEntries = _a.sent();
                        return [3 /*break*/, 5];
                    case 4:
                        err_1 = _a.sent();
                        if (err_1.code === 'ENOTDIR') {
                            return [2 /*return*/, results];
                        }
                        throw err_1;
                    case 5:
                        _i = 0, dirEntries_1 = dirEntries;
                        _a.label = 6;
                    case 6:
                        if (!(_i < dirEntries_1.length)) return [3 /*break*/, 10];
                        dirEntry = dirEntries_1[_i];
                        if (dirEntry === 'pango-component.js') {
                            results.push(path.join(relativePath, 'pango-component.js'));
                            return [3 /*break*/, 9];
                        }
                        p = path.join(dir, dirEntry);
                        return [4 /*yield*/, fs.lstat(p)];
                    case 7:
                        stat = _a.sent();
                        if (!(stat.isDirectory() || stat.isSymbolicLink())) return [3 /*break*/, 9];
                        return [4 /*yield*/, ComponentLoader.findComponentPaths(rootDir, path.join(relativePath, dirEntry))];
                    case 8:
                        r = _a.sent();
                        results.push.apply(results, r);
                        _a.label = 9;
                    case 9:
                        _i++;
                        return [3 /*break*/, 6];
                    case 10: return [2 /*return*/, results];
                }
            });
        });
    };
    ComponentLoader.loadComponent = function (projectOptions, componentPath) {
        return __awaiter(this, void 0, void 0, function () {
            var component, componentFullPath, targets, _i, _a, targetName, target;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        projectOptions.loadedComponents = projectOptions.loadedComponents || [];
                        if (typeof componentPath === 'string') {
                            projectOptions.logger.debug("Loading component " + componentPath);
                            componentFullPath = path.join(projectOptions.projectDir, componentPath);
                            try {
                                component = require(componentFullPath);
                            }
                            catch (err) {
                                throw new NestedError("Failed to load \"" + componentFullPath + "\"", err);
                            }
                            component.componentPath = componentPath;
                        }
                        else {
                            component = componentPath;
                            component.componentPath = 'instance';
                        }
                        if (!component.targets) return [3 /*break*/, 7];
                        targets = void 0;
                        if (!(typeof component.targets === 'function')) return [3 /*break*/, 2];
                        return [4 /*yield*/, component.targets(projectOptions)];
                    case 1:
                        targets = _b.sent();
                        return [3 /*break*/, 3];
                    case 2:
                        targets = component.targets;
                        _b.label = 3;
                    case 3:
                        _i = 0, _a = Object.keys(targets);
                        _b.label = 4;
                    case 4:
                        if (!(_i < _a.length)) return [3 /*break*/, 7];
                        targetName = _a[_i];
                        target = targets[targetName];
                        return [4 /*yield*/, projectOptions.addTarget(targetName, target, component)];
                    case 5:
                        _b.sent();
                        _b.label = 6;
                    case 6:
                        _i++;
                        return [3 /*break*/, 4];
                    case 7:
                        projectOptions.loadedComponents.push(component);
                        return [2 /*return*/];
                }
            });
        });
    };
    return ComponentLoader;
}());
exports.ComponentLoader = ComponentLoader;
//# sourceMappingURL=ComponentLoader.js.map